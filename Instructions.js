const MOVL_R1 = 0x10;
const MOVL_R2 = 0x11;
const ADDRR = 0x12;

module.exports = {
    MOVL_R1,    // Move literal into r1
    MOVL_R2,    // Move literal into r2
    ADDRR       // Add register to register
}